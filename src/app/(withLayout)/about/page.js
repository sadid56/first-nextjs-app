import React from 'react';

export const metadata = {
    title: 'About',
    description: 'This is about page',
  }
const About = () => {
    return (
        <div>
                <h2>about with lay  out</h2>
        </div>
    );
};

export default About;